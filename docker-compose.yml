# name: books
services:

  app:
    image: finkord/books-api:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    # depends_on:
    #   - postgres
    # env_file:
    #   - .env
    # volumes:
    #   - ./app:/app
    # networks:
    #   - books_network

    # postgres:
    #   image: postgres:16
    #   restart: unless-stopped
    # env_file:
    #   - .env
    # volumes:
    #   - ./data/psql:/var/lib/postgresql/data
    #   - ./configs/psql/:/docker-entrypoint-initdb.d
    # networks:
    #   - books_network


    # networks:
    #   books_network:
    #     driver: bridge
